services:
  - type: web
    name: eriot
    env: python
    pythonVersion: "3.13"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    autoDeploy: true
    # healthCheckPath: /health # Adicione se tiver um endpoint de health check